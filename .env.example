# ZipFix.ai Environment Variables

# Database
MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>/zipfix?retryWrites=true&w=majority

# JWT Secret (use a secure random string in production)
JWT_SECRET=your_jwt_secret_key_here

# Server Configuration
NODE_ENV=production
PORT=5000

# Google Maps API (for frontend)
REACT_APP_GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Optional: Frontend URL for CORS (Heroku will auto-configure)
CLIENT_URL=https://your-app-name.herokuapp.com
