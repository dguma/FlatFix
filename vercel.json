{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
  { "src": "/api/(.*)", "dest": "https://zipfix.onrender.com/api/$1" },
    { "src": "/(.*)", "dest": "/build/$1" }
  ]
}
