{
  "name": "flatfix",
  "version": "1.0.0",
  "description": "FlatFix - On-demand tire services frontend application",
  "main": "index.js",
  "scripts": {
  "dev": "npm start --prefix client",
  "build": "npm install --prefix client && npm run build --prefix client && rm -rf build && cp -r client/build build",
  "start": "npm start --prefix client",
  "test": "npm test --prefix client",
  "postinstall": "npm --prefix server install && npm --prefix client install"
  },
  "keywords": [
    "tire-service",
    "frontend",
    "react",
    "roadside-assistance"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
  "node": "20.x",
  "npm": "10.x"
  }
}
